/** Интерфейс данных элемента "Шаг". */
export interface IStepData {
    /** Идентификатор элемента. */
    id: number;

    /** Стиль иконки. */
    iconStyle: string;

    /** Заголовок. */
    title: string;

    /** Описание. */
    description: string;
}
